import { defineRecipe } from '@pandacss/dev'

export const badge = defineRecipe({
  name: 'badge',
  jsx: ['Badge'],
  base: {
    display: 'inline-flex',
    alignItems: 'center',
    borderRadius: 'sm',
    fontWeight: 'medium',
  },
  variants: {
    variant: {
      solid: { bg: 'primary', color: 'primary-fg' },
      subtle: { bg: 'bg.muted', color: 'fg.muted' },
      outline: { border: '1px solid', borderColor: 'border.default' },
    },
    size: {
      sm: { px: 2, py: 0.5, textStyle: 'xs' },
      md: { px: 2.5, py: 1, textStyle: 'sm' },
    },
  },
  defaultVariants: {
    variant: 'subtle',
    size: 'sm',
  },
})
